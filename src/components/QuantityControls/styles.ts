import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 4,
    borderRadius: 6,
  },

  button: {
    width: 36,
    height: 36,
    alignItems: 'center',
    justifyContent: 'center',
  },

  count: {
    width: 20,
    textAlign: 'center',
  },
})
